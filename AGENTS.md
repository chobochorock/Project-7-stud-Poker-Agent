# Shared Workspace Instructions

Read and follow [the workspace AGENTS.md](../AGENTS.md), including the daily
idea-record rule. Store the shared dated notes in `../아이디어/`, not this
project's root. Preserve other agents' sections and distinguish completed
experiments from hypotheses and work still running.

## Abstraction Comparison Reports

Always overlay the existing compatible k-means baseline in abstraction learning
curves. Include log-log Local BR-gap and model/baseline ratios at shared measured
budgets; separate current and average policies. Match evaluator roots, particles
and seeds or explicitly label the reference as unmatched. Never silently omit a
missing baseline. Do not extrapolate crossings from fitted slopes, hide negative
LBR values on log axes, or conflate these metrics with exploitability. Record
remaining training-data/key-partition differences even after matching evaluation.
