# 추가 기능 아이디어

이 문서는 현재 로컬 웹 MVP 이후에 추가하면 좋은 기능들을 정리한 목록입니다. 당장 모두 구현할 필요는 없고, 우선순위를 정해 단계적으로 붙이는 것을 권장합니다.

## 1. 게임 진행 기능

- 고정 스택 캐시 모드(구현됨)
  - 매 라운드를 1000칩으로 다시 시작하고 라운드별 chip net을 세션 누적 손익에 합산합니다.
  - 캐시 에이전트 평가는 핸드 승률뿐 아니라 평균 chip net을 함께 사용합니다.

- 여러 라운드 연속 진행(우선, 구현됨)
  - 카드 딜링부터 쇼다운까지의 한 단위를 라운드라고 부릅니다.
  - 다음 라운드 시작 버튼을 추가했습니다. 칩이 0인 플레이어의 영구 탈락은 토너먼트 모드에서만 적용합니다.

- 토너먼트 에피소드 종료 조건(구현됨, 후순위)
  - 한 명만 칩을 보유하면 에피소드 종료로 표시합니다.
  - 최종 승자, 총 진행 라운드 수, 누적 손익을 보여줍니다.

- 자동 라운드 진행(구현됨)
  - 웹 설정에서 `Auto Next`를 켜면 지정한 시간 뒤 다음 라운드를 자동 시작합니다.

- 기본금 상승 규칙
  - 일정 라운드 수마다 기본금이 증가하도록 설정할 수 있습니다.
  - 예: 10라운드마다 1 -> 2 -> 4 식으로 증가.

- 되돌리기보다는 복기
  - 포커 게임 특성상 실제 진행 중 되돌리기보다는, 끝난 뒤 액션 기록을 다시 보는 기능이 더 적합합니다.

## 2. 웹 UI 개선

- 사람 플레이어 시점 선택
  - 현재는 human 플레이어의 비공개 카드가 보입니다.
  - 나중에는 “내가 어느 플레이어인지”를 선택하고, 그 플레이어의 비공개 카드만 보이게 할 수 있습니다.

- 현재 턴 강조(우선)
  - 행동해야 하는 플레이어 카드 영역에 강조 효과를 줍니다.
  - 예: 테두리 색, 턴 배지, 액션 패널 고정.
  - (패 회전 순서를 보여주는 것이 필요, Dealer라는 표시를 통해서 우선권을 지닌 사람을 표현)

- 베팅 버튼 설명(우선)
  - `BBING`, `QUARTER`, `HALF`, `FULL`이 실제 몇 칩인지 버튼에 같이 표시합니다.
  - 예: `HALF (+42)`, `CALL (17)`.

- 카드 디자인 개선
  - 현재는 텍스트 카드입니다.
  - 추후 카드 이미지 또는 더 정돈된 카드 컴포넌트로 바꿀 수 있습니다.

- 모바일 대응 개선
  - 현재도 기본 반응형은 있지만, 작은 화면에서 액션 패널을 하단 고정 패널로 바꾸면 더 편합니다.

## 3. AI와 학습 기능

- 규칙 기반 에이전트 추가(기능 이전, UI에서는 할 필요 없음)
  - 완전 랜덤보다 조금 더 그럴듯한 기준 에이전트를 만듭니다.
  - 예: 공개 패가 강하면 공격, 콜 금액이 너무 크면 폴드.

- `HA1` card-only belief 에이전트(구현됨)
  - 모든 보이지 않는 카드를 중복 없이 공동 표본 추출하여 showdown equity를 계산합니다.
  - 학습과 상대별 성향은 사용하지 않으며, 현재는 정확한 ISMCTS가 아니라 chance rollout과 고정 베팅 규칙을 사용합니다.

- 보수적 에이전트 / 공격적 에이전트
  - `agent/` 폴더의 새 파일에 각각 구현하고, `agent/base.py`의 에이전트를 상속합니다.
  - CLI와 웹에서 선택할 수 있도록 `main.py`, `web_controller.py`에 등록합니다.

- 학습 DB 요약 화면(우선, 그러나 DB의 구성을 우선적으로 정한 후 시도)
  - 저장된 에피소드 수, 상태 수, 행동별 평균 보상 등을 웹에서 확인합니다.

- 오프라인 학습 실행 명령
  - 게임 실행 없이 DB만 읽어서 모델 또는 통계 테이블을 갱신하는 스크립트를 추가합니다.

### 핸드 테이블과 belief state

- 홀덤의 시작 패 169칸 표를 그대로 옮기기보다, 거리별 `hand state -> equity/value` 조회표를 만듭니다.
  - 키에는 거리, 내 비공개/공개 카드, 상대 공개 카드, 생존 인원과 이미 확인된 dead card를 포함합니다.
  - 문양에는 우열이 없으므로 문양 이름을 등장 순서대로 정규화하여 같은 형태의 패를 하나의 키로 합칩니다.
  - 베팅 기록과 팟/스택은 카드 equity 표와 분리합니다. 카드 표를 작게 유지하고 정책 모델이 베팅 문맥을 별도로 처리하게 합니다.

- belief state는 `공개 상태 + 각 플레이어의 가능한 비공개 패 확률분포(range)`로 정의합니다.
  - 1단계는 보이지 않은 카드가 균등하다고 가정한 card-only range이며, `agent/hand_range.py`에 구현했습니다.
  - 상대 초반 히든 2장 조합은 전수 열거하고 상대 discard, 마지막 히든과 미래 공개 카드는 조합별 Monte Carlo로 적분합니다(구현됨).
  - 전체 승·무·패, split equity, 상대 최종 족보 분포와 조합별 조건부 equity를 반환합니다(구현됨).
  - 2단계는 베팅과 최초 discard/reveal 선택을 likelihood로 사용해 posterior를 갱신합니다.
  - 상대의 이름이나 에이전트 종류는 사용하지 않고, 좌석과 익명 행동 기록만 사용합니다.
  - `my_discarded_card`를 관측에 추가하여 자신이 아는 dead card를 range에서 제외합니다(구현됨).

- 마지막 히든카드는 제외하지 않고 chance 변수로 적분합니다.
  - 배분 직전에는 남은 덱에서 무작위이므로 별도의 행동 이력이 필요하지 않습니다.
  - 배분 뒤 마지막 베팅 행동에는 그 카드의 정보가 반영되므로, 7번째 거리의 행동까지 관측하면 posterior가 달라집니다.

- 상태 공간은 홀덤보다 반드시 작지는 않습니다.
  - 홀덤의 상대 비공개 카드는 2장이지만 이 게임은 마지막 거리에 상대 비공개 카드가 3장입니다.
  - 다인전에서는 플레이어별 range가 남은 덱을 공유하므로 가능한 공동 배분 수가 크게 늘어납니다.
  - 대신 공개 카드가 최대 4장이고 discard/reveal 및 베팅 이력이 있어 posterior를 좁힐 단서는 더 많습니다.

### 핸드 range UI(1차 구현 완료)

- 헤즈업 4구 이후에 관측자와 조합당 표본 수를 선택하고 명시적으로 계산할 수 있습니다.
- 상대 히든 2장을 실제 카드 기준 52×52 삼각 격자로 표시합니다. 색은 조합별 내 equity이며 known/dead card가 포함된 조합은 비활성화됩니다.
- 전체 승·무·패, split equity, 95% 근사 구간, 유효 조합/샘플 수와 상대 최종 족보 분포를 표시합니다.
- 격자 칸을 선택하면 해당 조합의 승·무·패, equity와 균등 prior를 확인할 수 있습니다.
- 현재 화면은 `Uniform card-only belief`임을 명시합니다. 베팅과 discard/reveal likelihood를 반영한 `action-conditioned` posterior는 차후 구현합니다.
- 다인전 공동 range와 학습된 posterior 비교 화면은 실제 분석 요구가 생길 때 추가합니다.

### MCTS와 GRPO 후보

- `agent/uct_agent.py`와 `uct_rollout.py`에 헤즈업 EV root-sampling UCT 기준선을 구현했습니다.
  - sampled 상대 히든은 node key에 넣지 않으며, 매 의사결정에서 현재 observation부터 fold/showdown까지 직접 rollout합니다.
  - 첫 상대 simulation 정책은 균등 무작위이며 search 결과는 별도 `uct_nodes` 테이블에 저장합니다.
  - 상대도 탐색하는 adversarial ISMCTS, action-conditioned belief와 subtree 재사용은 아직 구현하지 않았습니다.
- MCTS는 완전정보 게임처럼 실제 hidden card 하나를 정답으로 고정해 탐색하면 strategy fusion 문제가 생길 수 있습니다.
  - 첫 구현은 정보집합 Monte Carlo 탐색 또는 public belief state 탐색으로 제한합니다.
  - ReBeL의 이론적 보장은 2인 제로섬 조건이므로 최대 5인인 현재 게임에 그대로 적용된다고 가정하지 않습니다.
  - 탐색 트리 자체는 매 행동 뒤 버려도 되지만, 학습에 사용할 경우 root visit policy, root value, belief 요약은 trajectory에 저장합니다.
  - 현재 random-opponent UCT의 처리량과 policy 안정성을 기준선으로 삼고 이후 adversarial ISMCTS로 확장합니다.

- GRPO는 같은 정보 상태에서 여러 rollout을 만들고 그룹 내부 보상으로 advantage를 정규화하는 실험 후보입니다.
  - rollout 원문을 영구 저장할 의무는 없지만, 업데이트 중에는 같은 상태에서 나온 그룹과 보상이 필요합니다.
  - 재현이 필요하면 전체 탐색 트리 대신 seed, 정책 버전, 행동열, 최종 보상과 그룹 통계만 저장합니다.
  - 긴 에피소드와 다인전의 높은 보상 분산 때문에 첫 기준선으로는 기존 Monte Carlo/정책경사 또는 CFR 계열을 먼저 비교합니다.

참고 자료:

- [DeepStack: Expert-level artificial intelligence in heads-up no-limit poker](https://pubmed.ncbi.nlm.nih.gov/28254783/)
- [ReBeL: Combining Deep Reinforcement Learning and Search for Imperfect-Information Games](https://proceedings.neurips.cc/paper/2020/hash/c61f571dbd2fb949d3fe5ae1608dd48b-Abstract.html)
- [DeepSeekMath: Group Relative Policy Optimization](https://arxiv.org/abs/2402.03300)

## 4. 복기와 로그

- 판별 상세 표시(우선)
  - 쇼다운 때 각 플레이어의 최종 족보를 사람이 읽을 수 있는 이름으로 보여줍니다.
  - 예: 트리플 A, 투페어 K/Q, 백스트레이트.

- 액션 타임라인(자세한 설명 필요)
  - 거리별로 베팅 로그를 접고 펼칠 수 있게 만듭니다.
  - `4th`, `5th`, `6th`, `7th_hidden`, `showdown` 단위로 구분합니다.

- 복기 파일 저장(우선, 구현됨)
  - 캐시 세션 또는 토너먼트 에피소드의 전체 상태, 라운드별 액션, 결과를 하나의 JSON 파일로 저장합니다.
  - 파일명은 `{시간}_{에이전트 정보 개요}.json` 형태를 사용합니다.
  - 추후 “복기자 모델”이 읽기 좋은 형태로 만들 수 있습니다.
  - 파일들은 `replays/` 폴더에 따로 보관합니다.

- 리플레이 모드(복기 파일을 통해서 실행 가능해야 함)
  - 끝난 라운드를 한 액션씩 넘겨보는 화면을 만듭니다.

## 5. 안정성 및 테스트

- 웹 API 테스트 강화
  - `/api/start`, `/api/action`, `/api/discard`, `/api/state`를 직접 테스트합니다.

- 사람 플레이어 시나리오 테스트
  - 사람 1명 + 랜덤 4명 구성이 끝까지 진행되는지 확인합니다.

- 사이드팟 케이스 추가
  - 여러 명이 서로 다른 금액으로 올인하는 상황을 더 많이 테스트합니다.

- 공개 패 우선권 테스트 확대
  - 공개 포카드, 트리플, 투페어, 원페어, 하이카드 우선순위를 각각 확인합니다.

## 6. 구조 개선 후보

- CLI와 웹의 에이전트 생성 로직 통합
  - 현재 `main.py`와 `web_controller.py`가 각각 에이전트를 생성합니다.
  - 나중에 `agent_factory.py` 같은 파일로 합치면 새 에이전트 등록이 쉬워집니다.

- 출력 메시지 제어
  - 현재 랜덤 에이전트와 게임 환경이 콘솔에 진행 메시지를 출력합니다.
  - 웹 실행에서는 조용히 동작하도록 로그 옵션을 분리할 수 있습니다.

- 게임 진행 상태 머신 정리
  - 현재 `web_controller.py`가 웹용 진행 제어를 담당합니다.
  - 기능이 많아지면 상태 클래스를 더 세분화할 수 있습니다.

## 추천 우선순위

1. 베팅 버튼에 실제 칩 금액 표시
2. 현재 턴 플레이어 강조
3. 여러 라운드 연속 진행
4. 최종 족보 이름 표시
5. 규칙 기반 기준 에이전트 추가
6. 복기 파일 저장
7. 리플레이 모드
