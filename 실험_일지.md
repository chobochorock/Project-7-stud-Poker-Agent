# 7-Stud Poker Agent 실험 및 개념 일지

기간: 2026-07-01 ~ 2026-08-29  
작성 기준: 프로젝트 채팅, 소스 파일 수정 시각, 저장된 모델·JSON·CSV, 기존 설계 문서

## 1. 문서 목적과 기록 원칙

이 문서는 7-Stud Poker Agent를 만들면서 검토한 아이디어, 실제 구현, 정량 실험,
실패 원인과 다음 판단을 시간순으로 정리한다. 단순한 성공 사례 목록이 아니라,
어떤 가설이 어떤 실험으로 반박되거나 보강되었는지를 남기는 것이 목적이다.

채팅 원문에는 모든 메시지의 개별 날짜가 보존되어 있지 않다. 따라서 날짜가
파일·결과물로 확인되는 항목은 해당 날짜에 기록했고, 개념 논의만 남은 항목은
대화 순서와 인접한 구현 시각을 바탕으로 기간 단위로 묶었다. 수치가 없는
항목은 당시의 가설이며, 구현 또는 검증이 끝난 사실처럼 서술하지 않았다.

평가 수치의 기본 해석은 다음과 같다.

- `heuristic ante/hand`: 양수이면 목표 정책이 휴리스틱에게 이긴다.
- `policy-LBR ante/hand`: LBR이 목표 정책에서 얻은 이익이다. 낮을수록 좋다.
- LBR은 exact best response가 아니므로 exploitability의 근사 하한이다.
- `sampled BR-gap`: 학습 궤적 또는 표본 정보집합에서 측정한 국소 이탈
  여지다. exact exploitability나 인증된 상한이 아니다.
- 서로 다른 seed와 서로 다른 평가기에서 나온 point estimate는 신뢰구간 없이
  직접 순위를 확정하지 않는다.

## 2. 전체 요약

두 달간의 연구는 다음 일곱 단계로 진행됐다.

1. High-card toy no-limit 환경에서 `hand -> deck -> episode` 계층, 공통
   transition DB와 에이전트 인터페이스를 먼저 정의했다.
2. 정확 상태 rollout과 UCT 데이터를 만들고 MLP, k-means, GMM으로 일반화를
   시도했다.
3. 클러스터 정책이 휴리스틱에게 크게 패하면서 density 기반 clustering만으로
   전략을 만들 수 없다는 점을 확인했다.
4. Kuhn과 Stud-Leduc에서 CFR/CFR+를 검증한 뒤, 7-Stud에는 C++ external-sampling
   MCCFR과 고정 power bucket을 적용했다.
5. Hard MCCFR은 휴리스틱과 비슷하거나 앞서는 수준까지 갔지만 policy-aware
   LBR에는 약 `1~2 ante/hand`의 명확한 누수를 보였다.
6. Soft mixture, local temperature, adaptive split, 더 긴 betting memory와
   heuristic population atlas를 시험했지만 순정 hard bucket MCCFR을 안정적으로
   넘지 못했다.
7. 7th-street exact-history resolver, PBS/ReBeL, sampled BR-gap, Deep CFR로
   확장했다. 올바른 posterior를 사용한 깊은 7th 탐색은 유망했지만 평가
   fallback과 safe resolving 부재 때문에 해결된 정책으로 인정할 수 없었다.

가장 중요한 결론은 다음과 같다.

- 큰 게임에서 full CFR+ 대신 MCCFR이 필요한 판단은 맞았다.
- 현재 최고의 재현 가능한 blueprint는 `power512 + memory16 + hard MCCFR`
  계열이다.
- 문제는 단순 학습량만이 아니다. 같은 abstract bucket 안에서 regret은 거의
  작아졌는데 원 게임 LBR은 남아 있어 abstraction과 imperfect recall이 핵심
  병목으로 드러났다.
- Soft policy mixture가 regret table을 대신할 수 있다는 가설은 현재 실험에서
  지지되지 않았다. Bucket은 정책 혼합기보다 regret을 공유하는 정보 추상화로
  사용할 때 가장 안정적이었다.
- Betting history에 맞는 posterior range를 만든 후 7th를 깊게 푸는 방향은
  계산량과 실전 반응에서 가장 유망했다. 다만 joint policy coverage와 safe
  resolving이 필요하다.
- Deep CFR은 MLP 학습 자체보다 external-sampling traversal과 거대한 reservoir가
  병목이었다. 현재 예산에서 tabular hard MCCFR을 대체하지 못했다.

## 3. 날짜별 실험·개념 일지

### 2026-07-01 ~ 2026-07-14: 문제 정식화와 후보 방법 탐색

#### 문제의식

7-Stud를 단순 강화학습 환경으로 볼 것인지, 불완전정보 extensive-form game으로
볼 것인지부터 정리했다. CFR의 정보집합, counterfactual regret, regret
matching, 평균전략을 공부했고 Pluribus, Libratus, DeepStack, Deep CFR,
ReBeL의 구조를 비교했다.

#### 주요 개념

- CFR 자체는 포커 휴리스틱이 아니지만, 큰 포커에 적용하려면 card/action
  abstraction과 subgame solving 같은 도메인 설계가 필요하다.
- 상태를 비지도 학습으로 묶는 생각은 `state abstraction`, 포커에서는
  `information/card abstraction`에 해당한다.
- raw observation의 거리보다 행동 가치, 보상, 전이 또는 bisimulation에 가까운
  유사도가 중요하다.
- dot product, spherical k-means, GMM-EM, value-aware custom clustering을
  난이도 순서대로 비교하기로 했다.
- Gaussian component를 local expert로 보고 responsibility로 정책을 섞는
  Gaussian-gated mixture-of-experts를 검토했다.
- Gaussian 대신 bounded feature에 Beta, simplex belief에 Dirichlet 또는
  generalized Dirichlet을 쓰는 방안도 검토했다.
- Mahalanobis ellipsoid 또는 covariance 축 box 안에 Beta density를 넣고,
  support 밖 표본을 novelty buffer로 보내는 명시적 cover를 구상했다.
- 시간층별 cluster를 hard tree 또는 soft DAG로 연결하는 `cluster time-tree`와
  이후 GNN/message passing 가능성을 검토했다.

#### 당시의 핵심 가설

`관측 상태 -> 전략적으로 의미 있는 latent -> 가변 Gaussian/Beta atlas ->
local policy/Q expert -> online uncertainty-triggered search`가 장기적인 통합
구조가 될 수 있다고 보았다. 그러나 이 시점에는 CFR regret 회계와 soft
policy mixture가 충돌할 가능성을 충분히 검증하지 못했다.

#### 선행 toy no-limit 환경에서 확립한 구조

7-Stud에 바로 들어가기 전, no-limit과 불완전정보 학습을 분리해서 볼 수 있는
작은 카드 게임 세 종류를 설계했다.

- High-card: A부터 10까지 각 4장인 40장 덱, 순환 선 플레이어, no-limit
  check/call/raise/fold/all-in, 동률 pot split
- Chain high-card: 우선 `A가 10을 이긴다`는 예외만 추가한 순환 서열 실험
- Indian poker: 자기 패 대신 상대 패를 보고, 선택적으로 10-fold penalty 적용

이후 실험 범위는 high-card 하나로 줄였고 chain high-card와 Indian poker는
중단 상태로 두었다. 중요한 수정은 덱을 hand마다 재생성하지 않는 것이었다.
고정된 한 덱을 shuffle한 뒤 계속 소모하며, 덱이 비거나 한 명을 제외한 모두가
파산하면 episode가 끝난다. 덱을 다시 shuffle해 episode를 연장하는 확장에서는
다음의 포괄 구조가 된다.

```text
episode
  deck round 1
    hand round 1, hand round 2, ...
  deck round 2
    hand round 1, hand round 2, ...
```

- `hand round`: 새 패를 받고 betting과 showdown/fold가 끝날 때까지
- `deck round`: 한 번 shuffle된 덱이 소진될 때까지의 여러 hand
- `episode`: deck round들을 포괄하며 최종 stack 기준 보상을 확정하는 단위

`max-round`는 기본 종료 규칙이 아니라 무한 실행을 막거나 향후 deck reshuffle
멸망전을 제한하기 위한 확장 인자로만 취급한다. Hand 종료의 chip delta는
credit assignment를 빠르게 하는 보조 보상이 될 수 있지만, 이를 hand 안의
모든 행동에 똑같이 나누면 베팅 크기와 위험을 무시하는 편향이 생긴다. 최종
episode utility를 보존하면서 hand/deck return을 auxiliary target으로 쓰는
방향을 남겼다.

Deck round가 hand policy를 간접 조절하는 meta-policy도 제안했다. 예를 들어
남은 덱 구성, stack ratio와 episode 진행도에 따라 `aggressive`, `cold` 같은
option의 가중치를 고르고, 실제 fold/call/raise는 hand policy가 선택하는
계층이다. 카드 카운팅은 규칙을 스스로 추론하기를 기대하기보다 남은 rank
count를 관측에 주거나, deal/action history를 recurrent state로 압축해 belief를
만들어야 한다는 판단으로 정리했다.

데이터는 알고리즘별 학습값과 원 궤적을 분리하기로 했다. 공통 off-policy DB는
`observation, legal mask, action, behavior probability, reward, next observation,
episode/deck/hand id, terminal flag`를 저장하고 Q-value나 regret은 넣지 않는다.
Q table, CFR regret, neural checkpoint는 에이전트별 저장소로 분리한다. JSON
state key의 팽창을 피하기 위해 observation은 고정 encoder의 binary/vector
payload로 저장하고, SQLite는 인덱스와 메타데이터를 담당하게 하는 방향을
설계 원칙으로 삼았다.

### 2026-07-14: 초기 에이전트 인터페이스

`agent/learning_agent.py`, `agent/human_agent.py` 등을 정리하면서 환경과
에이전트의 최소 인터페이스를 만들었다. 이후 모든 실험에서 같은 게임 규칙을
공유하려는 기반 작업이었다.

### 2026-07-15: Stackless EV, 정확 상태 rollout, UCT 전환

#### Stackless heads-up EV 모드

스택과 올인을 제거하고 최종 chip EV만 학습하는 헤즈업 모드를 설계했다.
보상은 `최종 획득 칩 - 총투입 칩`, 할인율은 1로 두었다. 큰 팟의 의미를
바꾸는 reward clipping이나 로그 변환은 사용하지 않기로 했다.

첫 균등 rollout 30초 실험은 17,750 hands, 76,414 state-action records를
생성했다. 4th에서 7th로 갈수록 `FOLD` 때문에 coverage가 빠르게 줄었다.

#### 8시간 균등 rollout

- hands: `1,469,250`
- state-action records: `6,254,259`
- 재방문: `24회`, 약 `0.000384%`
- SQLite: 약 `760.8 MiB`
- 평균 처리량: `51.01 hands/s`

정확 상태 키는 사실상 매번 새로 생겼다. Exact Q table은 평균값을 개선하는
테이블이 아니라 append-only 데이터셋처럼 증가했다. 이 결과로 정확 상태
저장을 더 키우기보다 함수근사 또는 정보 추상화가 필요하다는 결론을 얻었다.

#### UCT/ISMCTS 검토

현재 정보집합에서 상대 hidden hand와 미래 chance를 표본화하고, root action에
simulation을 집중하는 UCT 수집기로 전환했다. 실제 hidden state를 트리 키에
넣으면 strategy fusion이 생기므로 canonical information-set key를 사용했다.
UCT는 최종 균형 솔버가 아니라 local search teacher로 두고, root visit와 Q를
증류 데이터로 저장하는 방향이었다.

이날 확립된 중요한 구분은 다음과 같다.

- lossless suit canonicalization과 손실 있는 bucketing은 다르다.
- uniform 상대를 고정한 Q/UCT는 그 상대에 대한 best response이지 Nash
  equilibrium이 아니다.
- fold equity는 rollout에 상대 fold가 포함되면 Q에 자동 반영되지만, random
  상대의 fold 확률은 실전 전략적 압박과 다르다.

### 2026-07-16: MLP, spherical k-means, diagonal GMM 기준선

`clustering_train.py`에 세 모델을 공통 데이터와 지표로 구현했다.

- Raw MLP: 667차원 입력을 `256 -> 128 -> 32` latent로 압축하고 policy/Q head
  학습
- Spherical k-means: 정규화 latent와 centroid의 dot product
- Diagonal GMM: whitening 후 batched EM과 top-k responsibility

833,839 UCT root에 대한 첫 전체 결과는 다음과 같다.

| 모델 | Q MAE | Q-best 일치 | visit 일치 | search-Q regret |
|---|---:|---:|---:|---:|
| Raw MLP | 22.6964 | 38.82% | 37.37% | 16.9287 |
| Spherical k-means hard | 23.0229 | 50.33% | 47.62% | 10.2032 |
| Spherical k-means soft top-4 | 22.8146 | 50.55% | 47.82% | 10.1224 |
| Diagonal GMM top-4 | 22.4156 | 50.52% | 47.78% | 10.1077 |

GMM은 10만 fit 표본에서 소수 대형 component와 다수 극소 component로
붕괴했다. Full-train EM은 component 사용 균형을 개선했지만 held-out regret은
k-means soft `10.1496`, GMM `10.2239`로 오히려 GMM이 나빴다.

실제 heuristic 상대 10,000-hand 결과도 k-means `-1.547`, GMM `-1.635
ante/hand`로 모두 명확히 패했다. 학습 target이 random-opponent UCT이고 H4가
random이라는 distribution mismatch가 크게 작용했다.

#### 해석

Clustering이 noisy Q head보다 action ranking을 안정화한 것은 사실이었다.
그러나 density likelihood가 전략 유사성을 보장하지 않았고, component 평균
정책이 equilibrium 학습을 대신하지 못했다. GMM의 내부 likelihood가 좋은
정책의 증거가 아니라는 점도 확인했다.

### 2026-07-17 ~ 2026-07-18: Adaptive UCT와 cluster-Q의 한계

UCT descendant 저장, UCT-vs-UCT 데이터 수집, adaptive confidence stopping,
cluster Q-learning을 추가로 실험했다. 한 root에서 terminal reward는 방문한
UCT node로 역전파되지만 기존에는 실제 decision root만 SQLite에 남았다는 점을
확인했고, 일정 방문수 이상의 descendant를 제한적으로 저장했다.

100-hand adaptive UCT pilot은 다음 비용을 보였다.

- 1,219 search roots
- 4,240,000 actual root simulations
- 평균 `3,478 simulations/root`
- elapsed `2,884초`
- 평균 최종 CI95 radius `211.7 ante`

큰 베팅 경로의 heavy tail 때문에 epsilon stopping이 충분히 작아지지 않았다.
`simulation 4096`을 크게 늘리는 것만으로는 효율이 낮았고, 한 자기 hand를
고정한 채 상대 hand를 순차적으로 바꾸며 nested confidence를 계산하는 아이디어를
남겼다.

이 시기에 현재 학습이 `UCT behavior data -> cluster policy`인 전형적인
off-policy 증류라는 점도 명확해졌다. On-policy self-play 갱신이 필요하지만,
데이터 생성 비용과 동시 학습의 비정상성 때문에 CFR 계열이 다시 중심 후보가
됐다.

#### Q-learning, 한 hand MCTS와 history 압축 가설

Tabular Q-learning이 heuristic보다 약했던 이유를 단순 상태 수 하나로 보지
않았다. No-limit bet size, 상대 정책의 비정상성, terminal 중심의 sparse
reward, 남은 덱과 betting history의 aliasing, 그리고 거의 재방문되지 않는
정확 state가 함께 작용했다. SARSA는 behavior policy에 더 보수적으로 맞지만
이 coverage 문제를 해결하지 못하며, PPO/GRPO도 observation과 legal-action
계약을 통일하기 전에는 공정한 비교가 어렵다고 판단했다.

Q table을 한 hand 상한의 information-set MCTS에 prior/leaf value로 넣는 조합은
실용적 후보로 남겼다. 이미 방문한 상태의 `Q, N`은 rollout cache처럼 쓰고,
새로운 local branch만 추가 탐색하는 방식이다. 다만 determinization 하나를
완전정보 상태처럼 탐색하면 strategy fusion이 생기며, 한 hand 최적화만으로
deck/episode 균형을 보장하지는 않는다.

유사 상태의 Q를 근사하는 기본식은 다음과 같이 정리했다.

```text
Q_hat(s, a) = sum_i K(s, s_i) Q(s_i, a) / sum_i K(s, s_i)
```

`K`에는 cosine/dot product, RBF-Euclidean, Mahalanobis, Hamming/Jaccard 또는
학습된 metric을 사용할 수 있다. 그러나 feature density만 가까운 상태보다
action-value, reward 또는 regret vector가 비슷한 상태를 묶어야 한다. 이
순간 clustering은 순수 비지도 학습이 아니라 value-aware representation
learning이 된다.

History를 그대로 받는 transformer와, 남은 카드 count·stack·public betting
state를 고정 크기 sufficient statistic으로 만드는 두 경로도 비교 대상으로
남겼다. `100:100`, `50:150`, `150:50` 같은 round scenario curriculum은
coverage에는 유용하지만 실제 on-policy reach와 reward를 바꾸는 bias가 있어
보류했다.

보상 폭발을 제어하기 위해 `1000 ante` stack/상한 게임을 도입했다. 이후
`1 chip = 1 ante`와 `1000 chip = 1 ante` 모델을 혼동하지 않도록 모델을
분리했다.

### 2026-07-19 ~ 2026-07-20: Kuhn CFR와 staged MCCFR

`kuhn_cfr.py`로 작은 불완전정보 게임에서 regret matching과 평균전략 수렴을
직접 검증했다. 이후 7-Stud 전체를 한 번에 풀기보다 7th table을 먼저 학습하고
6th+, 5th+로 점차 넓히는 staged MCCFR을 시도했다.

초기 Python 7th MCCFR은 100,000 hands에서 약 173,000 buckets를 만들었다.
마지막 bucket의 전략이 거의 fold였지만 `last_strategy`가 전체 평균이 아니라
마지막으로 방문한 정보집합의 정책임을 확인했다.

6th+가 동결된 7th 모델을 continuation으로 사용하는 방식도 구현했으나,
20,000-hand 평가에서 6th+ 모델이 7th 모델에 `-0.1123 ante/hand`, 95% CI
`[-0.1566, -0.0680]`로 패했다. 단계를 늘리는 것만으로 성능이 단조 개선되지는
않았다. 너무 세밀한 정보집합과 부족한 방문수가 원인이었다.

### 2026-07-21 ~ 2026-07-23: Bottom-up bounding과 고정 상대 POMDP

Toy environment에서 root에서 내려가는 UCT 대신 terminal/leaf return으로부터
행동별 confidence bound를 올리는 bottom-up bound를 구현했다. 고정 heuristic
상대에서는 POMDP best response가 빠르게 사람과 비슷한 value/call/fold 전략을
만들 수 있음을 확인했다.

다만 상대도 동시에 갱신하면 이 best-response 학습은 균형 수렴을 보장하지
않는다. 따라서 다음 역할 분담을 세웠다.

- 고정 상대 착취: bottom-up/POMDP best response
- 동시 self-play 균형: CFR 또는 saddle-point 방법
- 큰 게임 일반화: abstraction 또는 value network

### 2026-07-23 ~ 2026-07-24: Saddle point와 Asymmetric Perturbation

2인 zero-sum normal-form의 `x^T A y`와 asymmetric perturbation을 검토하고,
Kuhn과 3-player Kuhn의 gradient dynamics를 toy code로 실험했다. 3인 게임을
단순 trilinear tensor로 쓰는 것은 가능하지만, bilinear minimax의 monotone
operator와 수렴 정리를 그대로 가져올 수 없음을 확인했다.

PED, exploitability descent, strong Nash 같은 다인 안정성 개념도 검토했다.
결론은 saddle-point 가속은 작은 materialized subgame에는 유망하지만, 전체
7-Stud의 chance·정보집합 트리를 행렬 `A`로 만드는 것은 불가능하다는 것이었다.
7th public subgame에 제한해 적용하는 방향을 남겼다.

### 2026-07-24 ~ 2026-07-27: Stud-Leduc HRL과 규칙 v3

#### 2계층 HRL

Stud-Leduc C++ exact solver에 다음 모드를 구현했다.

- `flat`: 기존 CFR+
- `latent`: 라운드별 의미 없는 option 3개
- `semantic`: 목표 투자율 `{0, 0.5, 1}`
- `adaptive`: 목표의 이동·분할을 추가한 연구 모드

상위 정책은 최종 chip EV, semantic 하위 정책은 목표 투자율 오차를 reward로
받게 했다. Exact best response와 exploitability로 모든 모드를 평가했다.
Self-test의 flat CFR+ 200회 exploitability는 약 `0.00447`이었다.

목표 집합을 `{0, .25, .5, .75, 1}`로 조밀하게 하거나 자주 선택되는 목표를
분할하는 실험도 수행했다. 작은 exact game에서는 flat CFR+가 가장 빠르고
정확했으며, semantic hierarchy는 목표 달성도를 높일 수 있어도 원 게임
exploitability를 자동으로 낮추지 못했다. HRL은 정확 CFR이 불가능한 큰 게임에서
계산 구조를 제공할 때만 의미가 있다는 결론을 얻었다.

#### 피망식 5구 규칙 v3

실제 게임 관찰을 반영해 규칙을 확정했다.

```text
H4 -> 5th(플레이어당 최대 1회 raise)
   -> 6th(플레이어당 최대 2회 raise)
   -> 7th(플레이어당 최대 3회 raise)
```

`BBING` 뒤에는 `DDADANG`, check한 플레이어는 해당 round에서 raise할 수 없는
제약, H4 discard/reveal의 동시 선택도 반영했다. 이 변경으로 이전 규칙 데이터와
모델은 직접 재사용하지 않고 v3 계열을 새 기준으로 삼았다.

### 2026-07-27: C++ 7-Stud, exact CFR+ 기준선, fixed-root MCCFR

환경과 solver를 C++로 옮겨 Python 오버헤드를 줄였다. 작은 게임에서는 contiguous
array exact CFR+, 큰 7-Stud에서는 external-sampling MCCFR을 사용했다.

동일 training node visits에서 toy 결과는 다음과 같았다.

| 게임 | 알고리즘 | node visits | exploitability | 시간 |
|---|---|---:|---:|---:|
| Stud-Leduc rank 3 | Exact CFR+ | 10M | 0.005182 | 0.210s |
| Stud-Leduc rank 3 | MCCFR | 10M | 0.046758 | 2.502s |
| Mini-Stud rank 4 | Exact CFR+ | 20M | 0.024831 | 0.430s |
| Mini-Stud rank 4 | MCCFR | 20M | 0.044967 | 15.14s |

작은 트리에서는 full CFR+가 압도적이었다. MCCFR의 가치는 전체 순회 자체가
불가능한 7-Stud에서 나타났다.

Power64 fixed-root MCCFR의 heuristic 평가 곡선은 다음과 같았다.

| 누적 roots | heuristic ante/hand | 95% CI |
|---:|---:|---:|
| 1,000 | -3.8048 | [-4.3369, -3.2726] |
| 11,000 | -0.9941 | [-1.3553, -0.6328] |
| 31,000 | -0.1620 | [-0.3737, 0.0497] |
| 61,000 | -0.0836 | [-0.3000, 0.1328] |

61k roots, 약 74.1M node visits에서 휴리스틱과 통계적 무승부에 도달했다.
이것이 이후 hard MCCFR 기준선이 됐다.

#### pmang_v2 cluster policy 실패

동일 시기 GMM policy를 heuristic과 10,000 hands 평가한 결과는
`-4.9529 ante/hand`, 95% CI `[-5.8048, -4.1011]`였다. UCT Q/policy를
cluster mixture로 재생하는 경로는 최종 주력에서 제외했다.

### 2026-07-28 ~ 2026-07-29: LBR 도입과 adaptive soft cluster 실험

#### 평균전략 회계 수정

External-sampling MCCFR에서 average strategy를 traverser node에서만 누적하던
문제를 발견했다. 상대가 traverser인 순회에서 표본 경로로 방문한 opponent
node의 현재 정책을 누적하도록 수정했다. 수정 전 checkpoint와 수정 후 모델의
`strategy_sum`은 같은 의미로 비교하지 않았다.

#### Policy-aware LBR

기존 belief-BR은 수작업 action likelihood를 사용했다. 새 policy-LBR은 목표
MCCFR average strategy를 직접 조회해 hidden-hand posterior를 갱신했다.

```text
b_next(h) proportional to b(h) * sigma_target(observed_action | I(h))
```

초기 64-particle 평가에서 10M과 28.8M 모델의 LBR 이익은 각각 약 `1.748`,
`1.609 ante/hand`였다. 휴리스틱 대전에서는 보이지 않던 명확한 전략 누수였다.

#### Soft mixture와 adaptive growth

Hard table을 Gaussian responsibility top-p mixture로 바꾸고 local temperature를
조정하며, sampled one-step gap이 큰 위치에 새 centroid를 추가했다.

동일 100M node visits 결과:

| 방식 | heuristic EV | policy-LBR | 학습 특성 |
|---|---:|---:|---|
| hard | +0.0448 | 2.5056 | 가장 빠름 |
| fixed soft | -0.7640 | 4.8824 | 명확히 실패 |
| adaptive soft | -0.2069 | 2.5586 | fixed보다 회복, hard와 차이 불명확 |

Adaptive growth는 fixed soft보다 분명히 나았지만 hard를 넘지 못했고 약 7배
느렸다. 신규 cluster가 전부 7th에 생겨 global gap criterion이 payoff scale에
편향된 것도 확인했다.

Street-balanced threshold와 temperature-free mass/residual split도 실험했다.
100k roots에서 hard는 heuristic `-0.026`, LBR `1.549`, point split은
`-0.380/2.310`, residual split은 `-0.405/2.719`였다. 순정 hard MCCFR이
다시 가장 안정적이었다.

#### 개념적 결론

Cluster가 CFR에 주는 핵심은 초기 seed나 Q table이 아니라, 여러 원 정보집합이
누적 regret을 공유하게 만드는 정보 추상화다. 실행 정책을 soft mixture로 별도
변형하면 regret update가 최적화하는 정책과 실제 정책이 달라질 수 있다.

### 2026-07-30 ~ 2026-07-31: 스택, 5인 게임, H4와 warm start

20/50/100/200 ante heads-up stack sensitivity를 비교했고, 특히 20 ante에서
정책이 크게 흔들리는 것을 확인했다. 5인 cash에서는 각 플레이어 stack을
`50~1000 ante` log-uniform으로 독립 sampling하고, 자기 stack과 상대 stack
요약을 bucket feature에 넣는 실험 환경을 만들었다.

5인 평가는 다음 축으로 설계했다.

- 5 self-play
- 4 self-play + 1 heuristic/LBR
- 1 target + 4 heuristic
- 1 target + 정보 공유 없는 4인 team-LBR
- target 수 1~4의 field-strength curve

팀원은 private information을 공유하지 않고 공개 action으로만 신호를 보낸다.
같은 deal에서 target 자리를 순환하는 duplicate 평가를 필수로 두었다.

H4를 기존 100M blueprint와 분리해 학습했다. H4 100k 평가에서 평균
`-0.0252 ante/hand`, 95% CI `[-0.0632, 0.0128]`로 큰 손실 없이 heuristic과
비슷한 수준에 도달했다. K128 atlas로 100M 정책을 모방한 warm start와
regret-scale 조정도 실험했으며, 단순 policy copy보다 parent의 전략 질량과
regret 비율을 보존하는 초기화가 중요함을 확인했다.

### 2026-08-01: 7th AsymP oracle과 Deep CFR IPC 준비

7th는 미래 chance가 없어 exact terminal subgame을 만들 수 있으므로,
Asymmetric Perturbation과 saddle-point optimization의 첫 적용 구간으로
선택했다. 여러 perturbation scale의 oracle 결과를 저장했지만, 전체 trunk의
perfect recall과 safe resolving을 보장하지 못하므로 production solver로
승격하지 않았다.

Deep CFR는 traversal을 C++, 신경망 학습을 Python/PyTorch 프로세스로 분리하는
IPC 구조로 시작했다. LibTorch를 바로 도입하기보다 큰 배치로 sample을 넘기는
프로토타입 경로를 택했다.

### 2026-08-02 ~ 2026-08-04: K 증가, betting memory, heuristic population

#### K/epsilon ablation

Pair-or-better check/call, 그 외 fold를 regret prior로 두고 atlas 해상도와
epsilon-heuristic trajectory를 비교했다.

| 설정, 1M roots | LBR-240 | heuristic EV |
|---|---:|---:|
| K256, epsilon 0.2 | 1.1060 | +0.0521 |
| K256, epsilon 0.1 | 1.1477 | +0.0373 |
| K512, epsilon 0.2 | 0.9724 | -0.0113 |

K512는 LBR이 명목상 낮았지만 heuristic 성능은 유의하게 나빠졌다. 해상도를
올린다고 두 목적이 함께 개선되지 않았다.

#### Betting memory 4/16/81

Exact betting history는 bucket 폭발과 RAM 폭발을 일으켰다. 대신 이전 street의
공격 주도권을 몇 개 상태로 압축하는 memory4, 더 세밀한 memory16, 81-line을
만들었다.

Memory16 K512는 10M과 30M까지 학습했다. 같은 2,000-hand LBR-64 결과는
10M `0.937`, 30M `1.108`로 단조 개선되지 않았다. Memory81은 10k에서
`0.816`이었지만 100k `4.854`, 1M `2.278`로 불안정했다. 정책 풀을 늘려 atlas
coverage를 넓혀도 더 긴 history가 자동으로 균형 성능을 높이지 않았다.

#### Heuristic population atlas

기존 heuristic, 50% bluff-defense caller, 과도한 bluff, hidden-street bluff 등을
만들고 무작위 population trajectory로 atlas를 fit했다. 이는 휴리스틱 하나의
경로 편향을 줄이려는 시도였지만 memory81의 높은 차원과 부족한 방문수 때문에
결정적 개선으로 이어지지 않았다.

#### Conflict 진단

`reach(B) × conflict(B)`와 LBR bucket attribution을 기록하기 시작했다.

```text
conflict(B,a) = sum_t |r_t(B,a)| / (|sum_t r_t(B,a)| + epsilon)
```

Reach가 높고 regret 부호 상충이 큰 bucket은 서로 다른 전략적 상황을 합쳤을
가능성이 높다. 이후 split의 우선순위를 단순 density가 아니라
`reach × counterfactual conflict`로 두는 근거가 됐다.

### 2026-08-05 ~ 2026-08-09: 비균등 street atlas와 online split toy 검증

실제 방문량에 맞춰 street별 cluster 수를 `16/64/256`으로 다르게 배정한
atlas를 만들었다. 동일 총 cluster를 균등 배정하는 방식보다 데이터 기아를
줄이려는 목적이었다.

Toy Stud-Leduc와 limit high-card에서는 정적 coarse bucket과 adaptive split을
동일 node budget에서 비교했다.

초기 데이터는 uniform-random hand 2,000개였고, 각
`(player, round, legal-action set)`마다 bucket 하나로 시작했다. Split은 부모
영역 안에서만 일어나는 비가역 이진 트리이며 자식도 다시 split될 수 있다.
부모의 regret와 average-strategy mass를 자식 표본 비율로 나눠 즉시 정책을
보존했고 merge는 사용하지 않았다. Audit은 현재 정보 상태의 첫 행동만 각각
강제하고 이후 상태에서는 현재 bucket policy로 rollout하여 terminal reward
차이를 label로 사용했다.

| 게임, 300k visits | exact MCCFR+ | static bucket | adaptive bucket |
|---|---:|---:|---:|
| Stud-Leduc | 0.345 | 2.326 | 1.983 |
| Limit high-card | 0.044 | 0.850 | 0.633 |

Adaptive split은 static보다 나았지만 exact solver와는 큰 차이가 남았다.
10% random audit는 split 발견을 안정화했지만 학습 node의 절반 이상을 audit에
사용했다. 이로부터 early random audit와 later geometric audit를 섞는
schedule을 제안했다.

Geometric audit은 leaf 방문 `10, 100, 1000, ...`에서 action당 12회 rollout을
수행했다. Seed 41의 100k 방문을 10% random audit로 다시 실행한 결과는 다음과
같았다.

| 게임 | static | adaptive | leaves/splits | audit 비중 |
|---|---:|---:|---:|---:|
| Stud-Leduc | 2.215 | 1.772 | 48/36 | 65.1% |
| Limit high-card | 0.836 | 0.166 | 41/35 | 58.3% |

강한 audit는 유용한 split을 더 잘 찾았지만 비용이 너무 컸다. 따라서 새롭거나
오차가 큰 leaf에는 10%, 안정된 leaf에는 geometric schedule, 전체에는 약 1%
background audit를 유지하는 hybrid가 현재 가장 합리적인 후속안이다.

별도의 split-means notebook에서는 알려진 `k`를 쓰는 KMeans,
BisectingKMeans, Agglomerative와 `k`를 BIC로 고르는 GMM, 사용자 custom
split/merge hook을 같은 합성 Gaussian 데이터에서 비교했다. ARI, cluster
accuracy, inferred `k`, silhouette와 실행시간을 그래프로 저장했다. 이 실험은
기하학적 split 자체의 건전성을 보는 도구이고, reward/regret 기반 CFR bucket
검증은 위 online 실험이 담당한다.

### 2026-08-10 ~ 2026-08-11: 7th exact resolver와 local split

Memory16 30M blueprint를 유지하고 7th에 도달할 때마다 exact betting history와
선택적 hand history를 사용하는 local MCCFR resolver를 만들었다.

초기 LBR-240 비교는 다음 방향을 보였다.

| 설정 | LBR ante/hand | 95% CI |
|---|---:|---:|
| resolver 없음 | 약 1.299 | [1.107, 1.490] |
| iter 100, prior 100 | 약 1.223 | [1.021, 1.424] |
| iter 100, prior 1000 | 약 1.216 | [1.029, 1.403] |
| iter 10,000, uniform range | 약 0.934 | [0.274, 1.595] |

탐색을 깊게 하면 명목상 개선됐지만, uniform hidden range와 넓은 CI 때문에
결론은 약했다.

동시에 regret conflict를 기준으로 online local bucket split을 구현했다.
Touch inheritance, cooldown, capacity/depth limit와 bounded cache를 추가했지만,
capacity가 포화되면 split은 멈추고 audit cache만 증가해 RAM이 17GB까지
커지는 문제가 나타났다. 장기 run에는 split audit cache 상한과 비활성화가
필수라는 결론을 얻었다.

### 2026-08-11 ~ 2026-08-18: Partial ReBeL과 V7 value network

첫 ReBeL 계열 구현은 다음 구조였다.

```text
5th: 30M MCCFR blueprint
6th: particle local CFR + V7 leaf
7th: terminal local CFR
```

Public belief를 particle로 근사하고 7th type-wise target을 생성해 V7을
학습했다. 그러나 10k와 20k self-play hand checkpoint 결과는 다음과 같았다.

| V7 hands | labels | held-out MAE | LBR-240 |
|---:|---:|---:|---:|
| 10,000 | 31,896 | 5.2657 | 1.3764 |
| 20,000 | 64,168 | 5.1478 | 1.3664 |

데이터를 두 배로 늘려도 LBR 변화는 통계적으로 유의하지 않았다. 병목은 단순
label 수보다 joint private-range correlation 손실, 낮은 posterior ESS,
persistent PBS 부재, scalar V7 오차와 5th blueprint 고정에 있었다.

### 2026-08-19 ~ 2026-08-22: Exact PBS reference와 online ReBeL toy

#### Exact PBS reference

ReBeL 원형과 현재 구현의 차이를 분리하기 위해 양쪽 private type range,
Bayesian posterior update, compatible sampling, perfect-recall key를 포함하는
reference solver를 별도 작성했다. Full-depth exact PBS는 checkpoint가 빠르게
커져 512회에 약 119MiB였고, 10k까지 선형 증가하면 수 GiB가 예상됐다.

따라서 실제 확장 순서는 `7th exact leaf oracle -> type-wise V7 -> 6th
depth-limited search -> V6 -> 5th`로 정리했다.

#### Online ReBeL Stud-Leduc

작은 exact game에서 `PBS -> search -> CFV target -> 즉시 network update`를
end-to-end로 검증했다.

| 지표 | 첫 보고 | 마지막 |
|---|---:|---:|
| online exploitability | 0.8443 | 0.2378 |
| flat CFR+ exploitability | 1.3139 | 0.0306 |
| update 전 CFV MAE | 1.7947 | 0.2446 |
| update 후 CFV MAE | 1.1097 | 0.2126 |
| root regret proxy | 0.9128 | 0.1305 |

Online 구조는 실제로 학습됐지만 flat CFR+보다 느리고 exploitability 바닥도
높았다. 이것은 ReBeL의 실패라기보다 큰 게임용 search/value 구조가 작은 exact
game에서는 불필요한 근사 비용을 만든다는 결과였다.

### 2026-08-22 ~ 2026-08-23: Sampled BR-gap과 regret 회계

매 iteration마다 비싼 LBR을 실행하는 대신, external-sampling traversal에서
이미 계산한 action value를 사용해 trajectory BR-gap을 기록했다.

```text
g(q) = sum_I [max_a Q(I,a) - sum_a pi(a|I) Q(I,a)]
```

K512 cold-start 100k는 첫 10k `26.119`에서 마지막 10k `13.449`로 48.5%
감소했다. log-log 기울기는 약 `-0.309`였다. Heuristic gap은 약 `4.5`로
평평했다. 절대값의 대부분은 7th에서 발생했다.

고정 정책을 동일 10k deal에서 재평가한 결과:

| 모델 | trajectory/original | bucket-first |
|---|---:|---:|
| K512 cold-start 100k | 13.468 | 8.592 |
| Memory16 30M | 0.496 | 0.263 |

30M의 abstract/local regret은 매우 작아졌지만 과거 policy-LBR은 약 1 이상이었다.
이 차이는 `abstract game에서는 거의 stationary하지만 원 게임의 다단계 착취는
남는다`는 가장 강한 abstraction/imperfect-recall 병목 신호였다.

같은 지표에서 7th resolver iter 100은 blueprint `0.501`보다 `0.591`로 오히려
악화됐다. Local search의 action이 실전에서 좋아 보인다는 것과 sampled local
gap이 낮아진다는 것은 동일하지 않았다.

### 2026-08-23: Posterior 7th resolver와 평가의 함정

Uniform determinization 대신 H4 관측과 5th 이후 exact betting history를
blueprint likelihood로 replay해 상대 hidden-hand posterior를 만들었다.

```text
candidate hidden hand
  -> H4 compatibility filter
  -> history action likelihood under blueprint
  -> posterior weight
  -> weighted determinization
  -> 7th exact-history local MCCFR
```

10,000-hand LBR-240에서 처음 얻은 값은 `0.2033 ante/hand`, 95% CI
`[-0.1119, 0.5186]`였다. Uniform resolver보다 크게 좋아 보였고, 10,000-iteration
search에서 local node 생성도 약 91.8% 줄었다.

그러나 `blueprint_fallbacks = 3,270,000`을 확인했다. 실제 target hand로 만든
local table은 LBR이 질의하는 가상의 다른 target hands를 모두 포함하지 않았다.
즉 LBR이 본 정책과 웹에서 실행되는 hand-conditioned resolver 정책이 달랐다.
따라서 `0.203`을 실제 exploitability 하한으로 확정할 수 없었다.

Joint-public-root PBS resolver로 policy query를 일관되게 만든 1,000-hand
screening에서는 blueprint LBR `1.273`, joint-PBS iter100 `1.126`이었다. 방향은
개선이지만 신뢰구간이 크게 겹쳤다. 이 사건은 강한 수치보다 **평가기와 배치
정책이 동일한 counterfactual policy mapping을 노출하는지**가 먼저라는 교훈을
남겼다.

### 2026-08-23 ~ 2026-08-24: Deep CFR 감사, 100 iteration, compact reservoir

#### 구현 감사

기존 Deep CFR에서 다음을 교정했다.

- street별 고정 quota 대신 global uniform reservoir
- illegal action을 loss에서 제외
- LCFR iteration weight 안정화
- advantage target scale 조정
- residual MLP와 LayerNorm
- trajectory BR-gap 기록

Stud-Leduc OpenSpiel adapter의 행동 수를 chance outcome 6개가 아니라 실제
플레이어 action 3개로 수정했고, average-policy의 legal probability를 다시
정규화했다.

Toy paper-structure 실험 결과:

| 게임 | Deep CFR | exact CFR+ 200 |
|---|---:|---:|
| Kuhn exploitability | 0.031345 | 0.000295 |
| Stud-Leduc exploitability | 0.300551 | 0.004799 |

작은 게임에서는 Deep CFR가 정확 테이블보다 훨씬 비싸고 부정확했다.

구조 감사 결과 core는 OpenSpiel의 external-sampling traversal, 플레이어별
advantage memory/network, persistent reservoir, iteration-weighted loss,
strategy memory와 최종 평균정책이라는 원 논문의 큰 흐름을 따랐다.
`--paper-structure`에서는 advantage head의 0 초기화와 gradient clipping 1을
추가했다. 다만 network는 원 논문의 poker-specific 7-layer 구조를 그대로
복제한 것이 아니라 generic MLP였고, 위 수치는 seed 17 한 번의 비교이므로
알고리즘 간 일반적인 수렴률로 확대 해석하지 않는다.

#### 7-Stud corrected 100 iteration

- iterations: `100`
- traversals/player/iteration: `200`
- parameters/network: 약 `301,960`
- elapsed: `13,675초`, 약 3.8시간
- reservoir: global uniform, 50k 규모

Trajectory BR-gap은 iter10 `32.39`, iter45 `20.67`, iter95 `55.69`, iter100
`32.95`로 크게 진동했고 일관된 하강을 보이지 않았다. Reservoir가 대부분
7th 표본으로 채워지고 5th/6th 표본이 매우 희소한 것도 확인했다.

#### Compact reservoir

1832차원 dense one-hot float를 카드/history token과 legal bit mask로 무손실
저장했다. 한 slot을 약 `7,365 -> 103 bytes`, 약 71.5배 줄였고 dense tensor를
minibatch에서 정확히 복원했다.

논문 규모에 가까운 `T=100`, `K=3000`, advantage 2M/player, strategy 5M
reference를 시작했다. 5 iteration까지 약 15,401초가 걸렸고 trajectory BR-gap은
계속 7th에 집중되며 P0/P1 각각 수십 ante 수준으로 진동했다. MLP SGD가 아니라
매 iteration 수백만 node를 생성하는 C++ traversal이 총시간의 대부분이었다.

### 2026-08-25 ~ 2026-08-26: Abstraction 재검토

실제 포커 solver의 abstraction을 다시 검토하면서 다음 판단을 정리했다.

- Hand strength 하나가 아니라 future potential과 distribution-aware distance가
  필요하다.
- Betting history를 지나치게 압축하면 imperfect recall과 regret aliasing이
  발생한다.
- Exact history는 상태가 폭발하므로 perfect recall과 메모리 사이의 타협이
  필요하다.
- Card abstraction, action abstraction, subgame resolving은 별개의 오차원이다.
- FPIRA/CFR+IRA처럼 abstraction을 refinement하면서 전략 대응을 보존하는 기법이
  단순 k-means 재학습보다 이론적으로 적합하다.
- 온라인 search는 blueprint의 abstraction error를 국소적으로 보완할 수 있지만,
  trunk counterfactual value를 보존하는 safe resolving이 필요하다.

이 시점의 결론은 K만 `512 -> 1024`로 늘리는 것은 값싼 실험일 수 있지만,
LBR 정체를 해결할 충분한 근거는 없다는 것이었다.

### 2026-08-27 ~ 2026-08-28: CFR+/MCCFR 재평가와 Gaussian splatting 아이디어

#### CFR+와 MCCFR

CFR+는 regret update 규칙이고 MCCFR은 tree sampling 방식이므로 배타적 선택이
아니다. `MCCFR+` 조합도 가능하다.

- Full CFR+: 작은 fixed subgame에서 낮은 분산과 빠른 초기 수렴
- MCCFR: 전체 순회가 불가능한 7-Stud에서 현실적인 sampling
- MCCFR+: sampled regret를 즉시 0 아래에서 clipping하므로 초기 수렴을
  빠르게 할 수 있지만 sampling noise와 함수근사 오차에 민감
- Deep CFR: external-sampling MCCFR + neural regret approximation + LCFR weight

현재 7-Stud hard model은 이미 MCCFR을 사용한다. CFR+로 바꾸면 abstract game의
초기 최적화는 빨라질 수 있지만 memory16/power bucket의 추상화 바닥을 없애지는
못한다. 공정한 다음 실험은 같은 atlas, cold start, 같은 node visits에서
MCCFR과 MCCFR+를 비교하는 것이다.

#### Gaussian splatting과 bucketize

3D Gaussian Splatting의 렌더링 자체보다 adaptive density control의
`clone/split/prune`를 bucket refinement에 빌리는 아이디어를 검토했다.

```text
hard discrete partition:
  street / legal actions / betting memory

continuous Gaussian atlas:
  power vector / PBS / stack ratio

leaf payload:
  CFR regret and strategy mass
```

정책을 opacity처럼 합성하면 CFR regret 회계를 다시 왜곡할 수 있다. 대신
Gaussian score는 hard owner를 정하거나 split 후보를 찾는 데만 사용하고,
`reach × counterfactual conflict`가 큰 넓은 component를 장축 방향으로
분리하는 구조가 더 안전하다. 이 아이디어는 아직 7-Stud에서 검증되지 않았으며,
`regret-aware adaptive Gaussian atlas`라는 후속 가설로 남겼다.

### 2026-08-28 ~ 2026-08-29: Kuhn 정상형 행렬과 게임 트리 시각화

Kuhn Poker의 extensive-form game을 두 플레이어의 순수 전략 정상형으로
변환했다. 각 플레이어는 카드별 두 종류의 이진 decision을 가지므로 순수
전략은 각각 `2^6 = 64`개이고, P1 payoff matrix는 `64 x 64`가 된다.

```text
M[i, j] = (1/6) * sum_{c1 != c2} u1(strategy_i, strategy_j, c1, c2)
u2 = -u1
```

생성된 payoff 범위는 `-4/3`부터 `3/2`였고, 행렬 원소 multiset을 OpenSpiel의
`extensive_to_matrix_game(kuhn_poker)` 64 x 64 변환과 대조해 일치함을
확인했다. 브라우저에서 P1/P2의 여섯 contingent action, 64 x 64 heatmap,
선택한 셀의 게임 트리와 기대 payoff를 함께 볼 수 있게 만들었다. 로컬
`file://` 환경에서 theme CSS가 적용되지 않아 검게 보인 문제는 배경을
명시하고 모든 텍스트를 흰색 계열로 고정해 수정했다.

## 4. 주요 실험 결과 집계

### 4.1 성공 또는 보존할 기준선

| 항목 | 결과 | 판정 |
|---|---|---|
| Exact CFR+ on Stud-Leduc | 10M nodes, exploitability 0.005182 | 작은 게임 기준선 |
| Fixed-root hard MCCFR | 61k roots에서 heuristic과 통계적 무승부 | 7-Stud 첫 성공 |
| Power512 memory16 30M | abstract bucket BR-gap 0.263 | abstract game은 상당히 학습됨 |
| Policy-aware LBR | 숨은 취약성을 수치화 | 핵심 평가 도구 |
| 7th posterior resolver | 깊은 search의 node 수 대폭 감소 | 유망하나 평가 보완 필요 |
| Compact reservoir | 약 71.5배 무손실 저장 축소 | Deep CFR 인프라 성과 |
| Online ReBeL toy | PBS-search-CFV 학습 end-to-end 작동 | 구조 검증 성공 |
| Reward-audited local split toy | static 대비 exploitability 감소 | 유망하나 audit 비용 큼 |
| Kuhn 64x64 정상형 | OpenSpiel 변환과 원소 일치 | 시각적 검증 도구 |

### 4.2 실패 또는 현재 보류

| 항목 | 관측 | 이유 |
|---|---|---|
| Raw cluster policy | heuristic에 큰 패배 | off-policy UCT target, H4 mismatch |
| GMM-EM | k-means보다 전략 지표 개선 없음 | density와 value similarity 불일치 |
| Q-mixture greedy | 과도한 fold | component 평균에서 공격 가치 상쇄 |
| Soft policy mixture | hard보다 느리고 약함 | regret update와 실행 정책 불일치 |
| Local temperature | surrogate 개선이 LBR 개선으로 연결 안 됨 | 목적함수 misalignment |
| Adaptive split | fixed soft보다 낫지만 hard를 못 넘음 | 7th 편향, audit 비용, 비정상 성장 |
| Exact betting history | 수백만 bucket과 RAM 폭발 | 원 게임 정보집합 공간 자체가 큼 |
| Partial ReBeL V7 | 데이터 증가에도 LBR 정체 | PBS 손실, value error, fallback |
| Deep CFR 현재 예산 | heuristic 및 BR-gap에서 부진 | traversal·reservoir·street imbalance |

## 5. 연구 과정에서 정정된 오해

1. **Cluster 수를 늘리면 전략이 자동으로 좋아진다.**  
   K512는 일부 LBR을 낮췄지만 heuristic 성능을 악화시켰다. 더 촘촘한
   abstraction은 방문수 기아와 regret 분산을 함께 키운다.

2. **Soft mixture가 포커 혼합전략을 자연스럽게 만든다.**  
   혼합전략 자체는 필요하지만, 서로 다른 information bucket의 정책을 사후
   평균하는 것은 CFR이 학습한 평균전략과 다르다. Regret matching 안에서 혼합이
   형성돼야 한다.

3. **휴리스틱에게 이기면 균형에 가깝다.**  
   휴리스틱 EV는 활용 성능이고 LBR은 방어 성능이다. 두 지표가 반대로 움직인
   실험이 여러 번 있었다.

4. **Local BR-gap이 0이면 exploitability도 0이다.**  
   Sampled gap은 방문한 국소 이탈만 본다. 원 게임의 range, 다단계 이탈,
   abstraction aliasing을 놓칠 수 있다.

5. **Resolver iteration만 늘리면 해결된다.**  
   잘못된 uniform range를 깊게 푸는 것은 낭비다. Posterior range가 먼저이며,
   safe resolving과 counterfactual policy coverage가 뒤따라야 한다.

6. **Deep CFR는 MLP라서 빠를 것이다.**  
   실제 병목은 매 iteration의 게임 traversal, network query, sample 생성과
   reservoir다. Network SGD만 보면 작지만 전체 solver는 비싸다.

7. **CFR+와 MCCFR 중 하나를 골라야 한다.**  
   전자는 regret update, 후자는 sampling 방식이다. 큰 게임에서는 MCCFR 또는
   MCCFR+를, 작은 exact subgame에서는 CFR+를 사용할 수 있다.

8. **새 centroid를 추가하면 기존 cluster만 둘로 갈린다.**  
   Global centroid 재경쟁은 다른 cluster의 소유 영역까지 움직일 수 있다.
   부모 base 영역을 고정한 local route tree에서만 자식을 비교해야 기존
   abstraction을 국소적으로 refinement할 수 있다.

## 6. 현재 모델과 연구 상태

### 주력 blueprint

- 규칙: betting rules v3, heads-up, 5th부터 betting
- 게임 단위: `ante=1000`, stack `1000 ante`
- atlas: `cpp_mccfr/power512_epsheur20_memory16_v1.bin`
- model: `cpp_mccfr/made_call_r1000_k512_epsheur20_memory16_30m.bin`
- H4: heuristic 또는 별도 H4 table
- 학습: external-sampling hard MCCFR

### 현재 가장 중요한 진단

Memory16 30M의 bucket-first BR-gap은 `0.263`까지 낮지만 LBR은 약 1 이상이다.
따라서 같은 atlas에 roots를 무한히 추가하는 것보다 다음 순서가 타당하다.

1. 평가 정책과 LBR query policy를 완전히 일치시킨다.
2. 7th joint-PBS exact resolver와 safe-resolving gadget을 검증한다.
3. 5th/6th는 type-wise CFV network로 한 street씩 depth-limit을 확장한다.
4. 고정 atlas의 MCCFR vs MCCFR+를 같은 node budget에서 비교한다.
5. Dynamic refinement는 policy mixture가 아니라 regret-preserving hard split으로
   제한한다.
6. 5인 확장은 2인 방어 지표가 안정된 뒤 field curve와 team-LBR을 병행한다.

## 7. 재현성과 관련 문서

### 전체 계보와 이론

- [DESIGN_LINEAGE.md](DESIGN_LINEAGE.md)
- [CLUSTER_AGENT_THEORY.md](CLUSTER_AGENT_THEORY.md)
- [CFR_MCCFR_VALIDATION.md](CFR_MCCFR_VALIDATION.md)
- [FIVE_PLAYER_TRAINING_PLAN.md](FIVE_PLAYER_TRAINING_PLAN.md)

### Adaptive abstraction

- [cpp_mccfr/ONLINE_LOCAL_BUCKET_SPLIT.md](cpp_mccfr/ONLINE_LOCAL_BUCKET_SPLIT.md)
- [cpp_mccfr/EXPERIMENT_PLAN.md](cpp_mccfr/EXPERIMENT_PLAN.md)
- [Toy online split 결과](../Toy-Card-Game-Agent/ONLINE_BUCKET_SPLIT_RESULTS.md)
- [Adaptive cluster/temperature 기록](../Toy-Card-Game-Agent/ADAPTIVE_CLUSTER_TEMPERATURE_REFINEMENT.md)
- [Split-means notebook](../MyToyEnvironment/split%20cluster/split_cluster_experiment.ipynb)
- [Split-means 사용법](../MyToyEnvironment/split%20cluster/README.md)

### Resolver와 ReBeL

- [cpp_mccfr/SEVENTH_POSTERIOR_RESOLVER_KO.md](cpp_mccfr/SEVENTH_POSTERIOR_RESOLVER_KO.md)
- [cpp_mccfr/REBEL_7STUD_IMPLEMENTATION.md](cpp_mccfr/REBEL_7STUD_IMPLEMENTATION.md)
- [cpp_mccfr/REBEL_RECURSIVE_7STUD.md](cpp_mccfr/REBEL_RECURSIVE_7STUD.md)
- [cpp_mccfr/REBEL_EXACT_PBS_REFERENCE.md](cpp_mccfr/REBEL_EXACT_PBS_REFERENCE.md)
- [online_rebel_stud_leduc/RESULTS.md](online_rebel_stud_leduc/RESULTS.md)
- [rebel_7stud_checkpoints/RESULTS_KO.md](rebel_7stud_checkpoints/RESULTS_KO.md)

### 평가와 Deep CFR

- [br_gap_experiment/RESULTS_KO.md](br_gap_experiment/RESULTS_KO.md)
- [cpp_mccfr/REGRET_UPPER_BOUND.md](cpp_mccfr/REGRET_UPPER_BOUND.md)
- [DEEP_CFR_7STUD_AUDIT_2026-08-23_KO.md](DEEP_CFR_7STUD_AUDIT_2026-08-23_KO.md)
- [deep_cfr_compact/README_KO.md](deep_cfr_compact/README_KO.md)
- [Toy Deep CFR 논문 감사](../Toy-Card-Game-Agent/DEEP_CFR_PAPER_AUDIT.md)
- [Kuhn 정상형 행렬 시각화](../Toy-Card-Game-Agent/visualizations/kuhn-poker-matrix.html)

## 8. 최종 평가

이 기간의 가장 큰 성과는 완성된 초인적 7-Stud 정책이 아니라, 무엇이 실제
병목인지 단계적으로 분리한 것이다.

- 정확 상태 저장은 coverage를 만들지 못했다.
- Clustering만으로는 전략이 만들어지지 않았다.
- Hard abstraction + MCCFR은 실제로 강한 기준선을 만들었다.
- 휴리스틱 대전은 방어력을 측정하지 못했다.
- LBR은 abstraction 누수를 드러냈다.
- Soft mixture와 무분별한 split은 regret minimization을 대체하지 못했다.
- 정확 history는 좋지만 그대로 저장하면 폭발한다.
- Posterior와 local search는 유망하지만 일관된 policy oracle과 안전성이 필요하다.
- Deep CFR은 공간을 함수근사로 바꾸지만 계산과 표본 문제를 없애지 않는다.

따라서 다음 연구의 핵심 질문은 `cluster를 더 잘 만드는 법` 하나가 아니다.
정확히는 다음 세 오차를 분리해 줄이는 것이다.

```text
optimization error
  + information/action abstraction error
  + online resolving/value approximation error
```

이 세 항을 같은 평가기와 같은 연산 예산으로 추적할 수 있을 때, roots를 더
늘릴지, atlas를 바꿀지, subgame search나 Deep CFR로 넘어갈지를 근거 있게
결정할 수 있다.
